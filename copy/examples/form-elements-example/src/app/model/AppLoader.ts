import {XD_Loader} from '@uxdf/ioc-model';
import {User001} from "./data/services/User0001";

export class AppLoader {
  static load() {
    // Deprecated: logic moved to services (see Page1Service)
  }
}
